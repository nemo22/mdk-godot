# Script opcodes

Generated from [`tools/python/script_opcodes.json`](../tools/python/script_opcodes.json) by
[`gen_script_table.py`](../tools/python/gen_script_table.py). *Italic* operands have layouts that
depend on other operands (see `complex_operand()` in
[`tools/python/mdk_script_dis.py`](../tools/python/mdk_script_dis.py) and the notes in
[`scripts/`](scripts/)). See [scripts.md](scripts.md) for the interpreter.

| Op | Mnemonic | Operands | Flow | Summary |
| ---: | --- | --- | --- | --- |
| 1 | `set_restart` | — | next | Stores the current pc (address of the next opcode) in obj+0x108: every following frame (and every yield) restarts here. |
| 2 | `follow_path` | off32 path, u8 flags1, u8 flags2, u16 start_frame, u8 relative, origin: *complex* | next | Starts following the spline path record at path (u32 key count, then 40-byte keys: s32 frame, 3 f32 position, 2x3 f32 tangents): obj+0xec = path. flags1 bit0 -> obj+0x149 bit1 (turn the object along the path), bit1 -> obj+0x14b bit3 (path drives XY velocity, Z untouched). flags2 bit0 -> obj+0x149 bit2 (play once, stop at the end instead of looping), bit1 -> speed obj+0xe8 = -1 (backwards; start_frame 0 then means the last key). obj+0xf0 = start_frame. relative!=0: origin obj+0xf4..0xfc = position - spline(start_frame), else origin = the 3 floats. obj+0xe6 (stop frame) = -1, then the position is updated. |
| 3 | `anim_once` | off32 anim | next | Sets the animation and plays it once: anim points to a CMI record; if its first u32 is 0 the animation is looked up by the name that follows (arena_find_animation). If it differs from obj+0x114 (or the animation had ended, obj+0x118 == 0xFF00): obj+0x114 = anim, frame obj+0xe4 = -1, time obj+0xdc = -1, obj+0x118 = -1. Clears obj+0x148 bit3 (looping, tested by object_anim_update 0x43a89c): the animation stops on its last frame and obj+0x118 becomes 0xFF00 (ended). obj+0x118 >= 0 is reset to -1. (Named anim_loop in earlier notes: 1064 of its 3273 uses are followed by if_anim_done/if_anim_frame, never those of opcode 59.) |
| 4 | `command_objects` | u8 command, command_args: *complex*, u8 selector, selector_args: *complex* | next | Sends a command to other objects (0x440384). Commands: 1 = join formation around the sender (obj+0x11e=1); 7 = set their script: goto target (A!=0xFC) or gosub (A==0xFC, pushed on their stack) - their obj+0x108/0x230/0x10c = target, wait cleared, leader obj+0x138 = sender; 43 = move near target like opcode 43. Selectors: 2 all of type, 3 all objects (no name), 4 first of type, 5 of type with id (obj+0x146) == id, 6 of type within distance param with line of sight, 7 of type following me (obj+0x138==self), 8 all following me, 9 the linked object obj+0x2b8, 10 of type with obj+0x14 >= param. Receivers must be active, in the same arena, not dead, obey the sender's priority (obj+0x11a/0x11b), and for command 7 not already running that target (obj+0x10c). |
| 5 | `raise_573_91c` | f32 v | next | If 0x573a60 != 0 and 0x573a64 > 0: global 0x57391c = max(0x57391c, v). |
| 6 | `move_to_target` | — | next | obj+0x11e = 6: movement code moves toward the target position 0x57fc34 (Kurt or the decoy/target object). |
| 8 | `set_yaw` | s16 degrees | next | obj+0x4c (yaw) = degrees normalised to 0..360. |
| 9 | `stop_script` | — | end | obj+0x108 = 0 (script no longer run), gosub depth obj+0x248 = 0, timer obj+0x26c[0] = 0, then returns. |
| 10 | `if_count_objects` | pstr type_name, u8 count, action: *complex* | branch | Condition: the number of active objects in the same arena whose type name (obj+0xc) equals type_name and that this object may command (their leader obj+0x138 is 0, self, or has a lower obj+0x11a; and their obj+0x11b >= our obj+0x11a) == count. |
| 11 | `set_priority` | u8 v | next | obj+0x11a = v (command priority used by opcodes 4/10). |
| 12 | `goto` | repeat(u8 × [off32 target]) | goto | Goto: picks one of the N targets at random (rand(N), 0 when N<=1; the chosen offset 0 means null) and jumps: obj+0x108 = pc = target, obj+0x26c[depth] = 0. N is usually 1 (up to 11 in the data, i.e. a random choice). |
| 13 | `if_cheat_key` | action: *complex* | branch | Condition: global 0x57eac0 != 0 (a key/input flag, bit 0x40000000 of 0x491018) and 0x5742b8 != 0 (cheat/debug mode, toggled by typing "ityflvbg"). When true, 0x57eac0 is cleared after the action (key consumed). |
| 14 | `if_sees_kurt` | u16 range, u8 cone, action: *complex* | branch | Condition: 0x4605d0(obj, range, cone): Kurt within range, inside a cone around the object's yaw obj+0x4c (width grows with cone/distance) and a clear line of sight (BSP ray in Kurt's arena and 0x573a68). |
| 15 | `alarm` | — | next | obj+0x11e = 15: while in Kurt's arena the movement code plays an alarm sound every 32 frames and sets 0x573aec = 10 (see opcode 27). |
| 16 | `set_health` | u16 health | next, or yield when health is 0 | obj+8 (health, dword) = health and u16 obj+0x2a2 = health. health >= 65000: obj+0x21f = 1 (indestructible). health == 0: 0x43d6d4 kills the object (death script obj+0x110 becomes obj+0x108/0x230 if set, otherwise it explodes) and returns. |
| 17 | `if_anim_done` | action: *complex* | branch | Condition: no animation (obj+0x114==0) or the animation has ended (obj+0x118 == 0xFF00). |
| 18 | `if_timer` | f32 seconds, action: *complex* | branch | Timer: counter c = u16 obj+0x26c[depth]. If seconds*30 > c: c += g_frame_ticks (0x491e18) and condition false; else c = 0 and condition true. The counter is reset by every goto/gosub. |
| 19 | `anim_mark_ended` | — | next | obj+0x118 = 0xFF00 (animation considered ended; the next opcode 3/59 restarts even the same animation). |
| 20 | `stop_path` | — | next | obj+0xec = 0 (stop following the path). |
| 21 | `path_stop_at` | s32 frame | next | u16 obj+0xe6 = low 16 bits of the operand (4 bytes are consumed; scripts use -2 = stop at the current frame round(obj+0xf0), -1 = no stop frame, or a frame number): the path holds when round(obj+0xf0) reaches this frame. |
| 22 | `if_hit` | action: *complex* | branch | Condition: hit event obj+0x21e != 0 and != 0xFD. When true, obj+0x21e is cleared after the action. |
| 23 | `set_flag148_1` | u8 on | next | on!=0: obj+0x148 \|= 1 and dword obj+0x302 = 1; on==0: obj+0x148 &= ~1 and obj+0x54 = 0 (bit0 disables the automatic banking computed from yaw changes). |
| 24 | `set_id_and_name` | u8 n, pstr name | next | u16 obj+0x144 = n - 1; obj+0x140 = pointer to the string (label used by other code). |
| 25 | `set_string154` | pstr name | next | obj+0x154 = pointer to the string. |
| 26 | `set_string150` | pstr name | next | obj+0x150 = pointer to the string. |
| 27 | `if_alarm` | action: *complex* | branch | Condition: global 0x573aec != 0 (set to 10 each frame by an object in movement state 15 in Kurt's arena, see opcode 15). |
| 28 | `bomb_follow_path` | off32 path | next | If the player projectile 0x491ef0 exists, has type 4 (+0xd0) and flag +0xf8 bit0 clear: it follows the spline path (callback 0x4634ac, +0xe0 = path, +0xe4 = 0) and the flag is set. |
| 29 | `spawn_chain` | u8 count, pstr model, off32 script | next | Finds model in the model table 0x520a84 (80 x 0x88 bytes; not found: debug log "CreateChain %s not found"), then creates count objects in the arena at the object's position: model instance, obj+0x11c = 7, movement state obj+0x11e = 30 (chain), leader obj+0x138 = self, id obj+0x146 = remaining + highest id of my followers + 1, obj+0x108 = script (may be 0). |
| 31 | `set_parts_mask` | repeat(u8 × [pstr part]) | next | For each name, every model part (obj+0xc: +0x1c count, +0x20 parts of 0x5c bytes) with that name (or all parts for "ALL") gets its bit set in obj+0x2c8 (hidden/removed parts mask). |
| 32 | `clear_parts_mask` | repeat(u8 × [pstr part]) | next | For each name (or "ALL"): clears the part's bit in obj+0x2c8 unless it is locked in obj+0x2cc (parts blown off by opcode 129). |
| 33 | `if_path_frame` | u32 frame, action: *complex* | branch | Condition: no path (obj+0xec==0) or round(path time obj+0xf0) >= frame-1. |
| 34 | `if_no_leader` | action: *complex* | branch | Condition: obj+0x138 == 0 (no leader/commanding object). When true, also clears obj+0x11e after the action. |
| 35 | `set_flag148_4` | u8 on | next | obj+0x148 bit2 = (on != 0). |
| 36 | `set_flag148_2` | u8 on | next | obj+0x148 bit1 = (on != 0). |
| 37 | `if_flag14c_2` | action: *complex* | branch | Condition: obj+0x14c & 2 (flag set by engine code; meaning not identified). |
| 38 | `if_vel_z` | cond: *cond*, action: *action* | branch | If the own vertical velocity obj+0x30 passes the condition, perform the action. |
| 39 | `push_forward` | speed: *value* | next | Horizontal move accumulator (obj+0x294, obj+0x298) += value * (cos yaw, sin yaw). |
| 40 | `turn_yaw` | rate (deg/s): *value* | next | Yaw obj+0x4c += value * frame time, wrapped to [0,360). |
| 41 | `set_targetable` | u8 mode (0 off, 1 on, 2 on + obj+0x14a bit 7) | next | obj+0x149 bit 0 = (mode != 0), obj+0x14a bit 7 = (mode == 2); if the object is now not targetable and it is the object locked in sniper mode (0x573b84), sniper mode is left. |
| 42 | `if_hit_part` | part: *complex*, action: *complex* | branch | Condition: 0 < obj+0x21e <= part count and (name of hit part #obj+0x21e (obj+0xc -> parts, 0x5c bytes each) == part name, or part name == "ANY") (case-insensitive). When true and the first string was non-empty, obj+0x21e is cleared after the action. |
| 43 | `move_near_target` | f32 forward, f32 side | next | Only if the object is in Kurt's arena (obj+0x60 == 0x573a0c): destination obj+0x120/0x124 = target position 0x57fc34 offset by (forward, side)*0.01*distance rotated by the direction object->target, obj+0x128 = target z + obj+0x5c; obj+0x11e = 43, obj+0xec = 0, path planning 0x45a1dc, clears obj+0x2a0/0x2a1/0x2a4..0x2ac and obj+0x14c bit3. |
| 44 | `if_move_done` | action: *complex* | branch | Condition: obj+0x11e == 0 (the movement command set by opcodes 6/15/43/... has finished). |
| 45 | `if_target_dist` | cond: *cond*, action: *action* | branch | If the 3D distance from self (obj+0x10) to the target point 0x57fc34 (Kurt, or the active decoy) passes the condition, perform the action. |
| 46 | `find_cover_spot` | action: *action* | branch | Look for type-5 waypoints of the arena (arena+0x38 count, arena+0x3c 36-byte entries) 9..400 units away, no farther from self than Kurt and closer to Kurt than self, that Kurt cannot see (BSP segment test) but self can; one of the 3 nearest is chosen at random (nearer = likelier) as destination obj+0x120 with motion obj+0x11e = 43, else obj+0x11e = 0. If obj+0x11e == 43 afterwards, perform the action. |
| 47 | `if_chance` | f32 percent, action: *complex* | branch | Condition: rand(10000) < percent*100. |
| 48 | `if_chance_per_second` | f32 mean_seconds, action: *complex* | branch | Condition: rand(10000) < (g_frame_dt / mean_seconds) * 10000, i.e. fires on average once every mean_seconds. |
| 49 | `release_children` | u8 count, action: *action* | branch (loop) | Repeat count times: detach the active object of the same arena that is attached to self (other+0x138 == self, other+0x11e == 30) with the highest id (+0x144>>16) by clearing its +0x138 and +0x11e; each time none is found, perform the action as if a condition were true (0xFE takes the then branch, there is no else). |
| 50 | `set_max_speed` | speed: *complex* | next | obj+0x38 = value (max speed, default 50). |
| 51 | `set_accel` | accel: *complex* | next | obj+0x3c = value (acceleration, default 10). |
| 52 | `set_decel` | decel: *complex* | next | obj+0x40 = value (deceleration, default 15). |
| 53 | `set_speed` | speed: *complex* | next | obj+0x34 = value (current speed). |
| 54 | `if_dest_dist` | cond: *cond*, action: *action* | branch | If the distance from self to the move destination obj+0x120 (2D when obj+0x14c bit 1 is set, else 3D) passes the condition, perform the action. |
| 55 | `set_gravity` | v: *complex* | next | obj+0x48 = value: gravity in units/s^2 (default 32), applied to the vertical velocity obj+0x30 when obj+0x148 bit 1 is set (0x45e74c); also used by opcodes 189 and 229. |
| 56 | `add_health` | s16 delta | next, or yield when health drops to 0 | If health obj+8 < 65000: obj+8 += delta. If obj+8 <= 0: obj+8 = 0, 0x43d6d4 kills the object, return. |
| 57 | `if_not_sees_kurt` | u16 range, u8 cone, action: *complex* | branch | Negation of opcode 14. |
| 58 | `anim_fps` | fps: *complex* | next | obj+0xe0 = fps (animation frames per second, default 30). |
| 59 | `anim_loop` | off32 anim | next | Same as opcode 3 but sets obj+0x148 bit3: the animation loops (and never ends). |
| 60 | `face_target` | — | next | Yaw = direction from self to the target point 0x57fc34 (Kurt or the decoy), in [0,360). |
| 61 | `fire` | origin: *fire_origin*, pstr bullet_name, off32 script (0 = none) | next | Spawn a global model (CMI directory 1, table 0x520a84: 80 entries of 0x88 bytes; unknown name: 'Bullet %s not found' is logged and nothing happens) at the origin (mode 0: own point obj+0x1b0 + 12*index; else the centre of the named model part in world space). The new object gets motion 61 (obj+0x11e), flags obj+0x148 \|= 0x80820, the spawner's yaw and pitch, and the script at obj+0x108; its type script runs once in object_init. |
| 62 | `if_target_angle` | cond: *cond*, action: *action* | branch | Angle between the own yaw obj+0x4c and the direction to the target point, folded to 0..180 degrees; if it passes the condition, perform the action. |
| 63 | `set_flag148_10_inv` | u8 off | next | off!=0: obj+0x148 &= ~0x10; off==0: obj+0x148 \|= 0x10. |
| 64 | `wait` | seconds: *complex* | wait | Wait: obj+0x22c = seconds (0 is replaced by 1e-5, i.e. one frame), obj+0x230 = pc after the operand, then returns. script_run decrements obj+0x22c by g_frame_dt each frame and resumes at obj+0x230 once it is <= 0. |
| 65 | `set_var` | u8 src, u8 index, f32 value | next | Variable src/index = value. |
| 66 | `add_var` | u8 src, u8 index, f32 value | next | Variable src/index += value. |
| 67 | `if_var` | u8 src, u8 index, cond: *cond*, action: *action* | branch | If variable src/index (0 global, 1 arena, 2 own, other = linked object obj+0x2b8; index 0..3) passes the condition, perform the action. |
| 68 | `set_flag` | u8 src, u8 bit | next | Set bit (bit&31) in flag word src (0 global 0x573b5c, 1 arena+0x58, 2 own obj+0x244, 5 obj+0x312 when obj+0x14a bit 4 is set, other = linked object's +0x244). |
| 69 | `clear_flag` | u8 src, u8 bit | next | Clear bit (bit&31) in flag word src. |
| 70 | `toggle_flag` | u8 src, u8 bit | next | Toggle bit (bit&31) in flag word src. |
| 71 | `if_flag_set` | u8 src, u8 bit, action: *action* | branch | If bit (bit&31) of flag word src (0 global 0x573b5c, 1 arena+0x58, 2 own obj+0x244, 5 obj+0x312, other = linked object's +0x244) is set, perform the action. |
| 72 | `if_flag_clear` | u8 src, u8 bit, action: *action* | branch | If bit (bit&31) of flag word src is clear, perform the action. |
| 73 | `set_obey_level` | u8 v | next | obj+0x11b = v (accepts commands from senders whose obj+0x11a <= v). |
| 74 | `attach_to` | u8 a (-> obj+0x276), u8 b (-> obj+0x277), pstr name_id ("<model>_<id>") | next | Find the active object of the own arena whose "%s_%d" (model name, id) equals name_id (the last match wins) and link to it: obj+0x278 = obj+0x138 = it, motion obj+0x11e = 74 (follow/attach). |
| 75 | `stop_motion` | — | next | Cancel the current motion command: obj+0x11e = 0. |
| 76 | `set_death_script` | off32 script (0 = none) | next | obj+0x110 = script: where the script goes when the object is killed (object_die 0x43d670 jumps there instead of the default death). |
| 77 | `debug_msg` | u8 nonfatal, pstr text | next | Debug print: sprintf "%s:%d (%d,%d,%d,%d) \"%s\"" (name, id, x, y, z, yaw, text) into 0x4d1a50 (the debug text line drawn on screen in debug mode); when nonfatal == 0 it is also passed to 0x40a2d0, which only appends to debug.err when debugging is enabled (no quit). |
| 78 | `move_to` | f32 x, f32 y, f32 z | next | Start walking to a point: destination obj+0x120..0x128 = (x,y,z), motion obj+0x11e = 78, obj+0xec = 0, a detour waypoint obj+0x12c is planned around walls, stuck detection (obj+0x2a0..0x2ac, obj+0x14c bit 3) is reset. |
| 79 | `set_pos` | f32 x, f32 y, f32 z | next | Teleport: position obj+0x10..0x18 = (x,y,z). |
| 80 | `add_vel_local` | f32 a, f32 b, f32 up | next | Velocity += (-a*cos(yaw) - b*sin(yaw), -b*cos(yaw) - a*sin(yaw), up) (the engine's local-offset convention, also used by 235, 248 and 159). |
| 81 | `push_hit_dir` | u8 mode (1 = multiply by the frame time), speed: *value* | next | Velocity (obj+0x28, obj+0x2c) += value * (cos, sin)(obj+0x224), obj+0x224 being the direction of the last hit or explosion; value is multiplied by the frame time when mode == 1. |
| 82 | `set_friction` | v: *complex* | next | obj+0x44 = value: friction in units/s^2 (default 64) that slows the velocity obj+0x28..0x30 down every frame (0x45e810; horizontal only when obj+0x148 bit 1, gravity, is set). |
| 83 | `set_scale` | args: *value_or_ramp* | next | obj+0x58 (scale) = value; or, with the 0xFF form, move obj+0x58 toward target each time it runs (growing: *= 1 + rate*dt, shrinking: *= 1 - rate*dt*0.5), clamped at target. |
| 84 | `set_height_offset` | value: *value* | next | obj+0x5c = value (height offset added to z when aiming at or walking to a target). |
| 85 | `set_rolling` | u8 on | next | on != 0: if obj+0x148 bit 6 is clear, set it and copy the 3x3 rotation part of the matrix at obj+0xac into obj+0x302..0x325 - from then on the object's matrix is that saved matrix (rotated by the distance moved, radius obj+0x326) times the scale instead of yaw/pitch/roll; on == 0: clear obj+0x148 bit 6. |
| 86 | `spawn` | f32 x, f32 y, f32 z, pstr type_name, off32 script (0 = none) | next | Spawns an enemy of type type_name at absolute (x,y,z) in this object's arena (obj+0x60) with the next free instance number and the given script (new+0x108); nothing if the type is unknown. |
| 87 | `if_kurt_looks_at_me` | u16 min_range, u16 max_range, u8 cone, action: *complex* | branch | Condition: 0x460730: min_range <= distance to Kurt <= max_range, the object is inside a cone around Kurt's yaw (g_damp_yaw) and the line of sight is clear. |
| 88 | `set_move_x` | u8 mode | next | If mode != 2: movement command obj+0x11e = 'X' (0x58) with parameter obj+0x11f = mode; if mode == 2: obj+0x11e = 0 and obj+0x34 = 0. Always clears obj+0xec. |
| 89 | `play_sound` | u8 flags, position: *complex*, pstr sound | next | Plays a sound found by name (debug log "%s #%d sfx not found: %s"). flags&0x80 (2D): flags&3 = 0 play, 1 restart, 2 play if not playing, 3 nothing. Otherwise 3D (volume 0x7fff, 1.0, 50.0): 0 play, 1 stop+play, 2 play if not playing, 3 stop. Position: 0x10 = floats (the handle follows the object obj+0x10), 0x20 = slot point, 0x40 = fixed floats, none = object position (0x20000 mode if flags&4). flags&4: the voice is stored in obj+0x158 and obj+0x15c = sound name (see opcode 249). |
| 90 | `model_texture_frame` | pstr texture_name, f32 frame (1-based) | next | Finds the animated texture texture_name in the object's model (obj+0xc: +0x10 pointer array, +0x18 count, name at +0x28 of each entry) and sets its absolute frame to frame-1; nothing if not found. |
| 91 | `set_path_speed` | speed: *complex* | next | obj+0xe8 = value (path speed in frames per tick, negative = backwards). |
| 92 | `if_anim_frame` | u16 frame, action: *complex* | branch | Condition: animation ended (as opcode 17) or current animation frame (s16 obj+0xe4) >= frame-1. |
| 93 | `accel_toward_point` | f32 speed, f32 x, f32 y, f32 z | next | Velocity += (point - position) / (\|dx\|+\|dy\|+\|dz\|) * speed (Manhattan-normalized). |
| 94 | `goto_weighted` | repeat(u8 × [u8 weight, off32 target]) | goto | Weighted random goto: r = rand(sum of weights); jumps to the first entry whose cumulative weight exceeds r (same jump semantics as goto). |
| 95 | `gosub_weighted` | repeat(u8 × [u8 weight, off32 target]) | gosub | Weighted random gosub (same selection as opcode 94, same stack behaviour as opcode 252). |
| 96 | `if_kurt_in_rect` | f32 x_min, f32 y_min, f32 x_max, f32 y_max, action: *action* | branch | If Kurt's real position 0x5739c0 is inside the XY rectangle [x_min,x_max] x [y_min,y_max], perform the action. |
| 97 | `set_banking` | u8 enable | next | enable != 0 clears bit 0x80 of obj+0x148 (automatic roll obj+0x54 and pitch obj+0x13c from turning/moving); enable == 0 sets it and zeroes the roll obj+0x54. |
| 98 | `group_set_state` | u8 tri_group, u8 op | next | Changes flag bits 0x10/0x20 of the arena triangles of group tri_group (op 0 set both, 1/other clear both, 2 set 0x10, 3 clear 0x10, 4 set 0x20, 5 clear 0x20) and mirrors them in the arena group masks (0x20 -> arena+0x10c, 0x10 -> arena+0x110). |
| 99 | `group_on_hit` | u8 hit_mask, u8 tri_group (1-16), off32 script (0 = none) | next | Sets the hit trigger of triangle group g: arena+0x7c[(g-1)%16] = hit_mask, arena+0x8c[(g-1)%16] = script offset (raw). When a triangle of group g is hit with a hit type in hit_mask (0x40d560), the arena counter arena+0xcc[g-1] is increased and the script is run once in the scratch object 0x57fc40 (0x45c9a0: pc = g_cmi + offset, +0x60 = arena, +0x21d = hit parameter), with the hit position/direction in the light globals 0x4d5374/0x4d5358. |
| 100 | `arena_show` | pstr arena_name | next | Makes arena arena_name visible ('BSPShow'); NONE passes NULL. Fatal error if not found. |
| 101 | `aim_target` | — | next | Yaw toward the target point (only when the XY distance > 2) and pitch obj+0x13c = atan2(target.z + 3 - z, XY distance). |
| 102 | `if_path_done` | action: *complex* | branch | Condition: no path is being followed (obj+0xec == 0). |
| 103 | `if_kurt_in_box` | f32 x_min, f32 y_min, f32 z_min, f32 x_max, f32 y_max, f32 z_max, action: *action* | branch | If Kurt's real position 0x5739c0 is inside the axis-aligned box, perform the action. |
| 104 | `aim_target_inaccurate` | f32 accuracy (100 = exact) | next | Aim yaw and pitch at the target point like 101 (no distance check); unless accuracy == 100 add a random error: yaw += (rand(20)-10)*(100-accuracy)/(dist3d+0.1), pitch += (rand(20)-10)*(100-accuracy)/((dist3d+0.1)*4). |
| 105 | `turn_to_target_offset3d` | f32 turn_rate (deg/s), f32 accuracy (100 = exact), f32 offset_a, f32 offset_b, f32 offset_z | next | Like 235 with P.z = target.z + offset_z, and the pitch is also turned toward atan2(P.z - z + obj+0x5c, dist2d); random error unless accuracy == 100 (the pitch error is divided by 1 + dist2d*4); yaw and pitch each move at most turn_rate*dt. |
| 106 | `set_302` | f32 value | next | obj+0x302 = value (unaligned float in a per-kind union: distance threshold of the obj+0x14b bit 4 animation-speed logic, jump flight time of 229, first element of the rolling matrix of 85). |
| 107 | `set_loop_sound` | pstr sound_name (empty = none) | next | Stop the object's looping sound obj+0x158; obj+0x15c = name; if the name is not empty start that sound looping at the object's position (flags 0x2000e, volume 0x7fff, 1.0, 50.0). |
| 108 | `if_touching_kurt` | action: *action* | branch | Condition: while flying as a projectile (obj+0x11e == 61) the hit flag obj+0x14c bit 2; otherwise the own bbox obj+0x198 (scaled by obj+0x2c0) overlaps Kurt's bbox 0x5739f4 and so does one of the model part boxes not masked by obj+0x2c8. If true, perform the action. |
| 109 | `hurt_kurt` | u8 damage | next | Damage Kurt (0x46a604): scaled by the difficulty 0x57423e, ignored while invulnerable (0x573bd4 > 0) or in some states; lowers his health 0x574324 and raises the hurt flash 0x573b20. |
| 110 | `delete_self` | — | end | Free this object (sounds, model instance, back to the free list) and return from script_run at once. |
| 111 | `set_instance` | u32 instance (low 16 bits used) | next | Sets the object's instance number obj+0x146 (u16) from the low 16 bits of the 4-byte operand. |
| 112 | `teleport_player` | pstr arena_name, f32 x, f32 y, f32 z, f32 yaw | next | Empty arena_name: moves the player (position 0x5739c0, yaw 0x5739f0) at once within the current arena and flashes the screen. Otherwise schedules a teleport to that arena (0x573c80 arena, 0x573c84..0x573c90 x,y,z,yaw); unknown arena = fatal error, and the script is stopped (obj+0x108 = 0, depth 0, return). |
| 113 | `spawn_relative` | f32 dx, f32 dy, f32 dz, pstr type_name, off32 script (0 = none) | next | Spawns an enemy of type type_name at this object's position plus (dx,dy) rotated by its yaw (obj+0x4c) and plus dz. |
| 114 | `if_player_on_me` | action: *cond_action* | branch | Condition: the player stands on this object (0x573b84 == obj). |
| 115 | `if_wall` | f32 rel_angle, f32 distance, action: *cond_action* | branch | Condition: a segment from the object's position (z raised when obj+0x148 bit 2) towards yaw+rel_angle of length distance hits the arena geometry. |
| 116 | `flags_set` | u32 mask | next | obj+0x148 \|= mask. |
| 117 | `flags_clear` | u32 mask | next | obj+0x148 &= ~mask. |
| 118 | `anim_end_frame` | s32 frame | next | u16 obj+0x118 (end/hold frame of the current animation, <0 = none) = (low 16 bits of the operand) - 1; 4 bytes are consumed. |
| 119 | `if_count_alive` | pstr type_name, compare: *compare*, action: *cond_action* | branch | Counts living objects named type_name in the arena and branches on the comparison. |
| 120 | `set_pitch` | f32 pitch | next | Sets obj+0x13c (pitch, degrees). |
| 121 | `if_floor_below` | f32 depth, action: *cond_action* | branch | Condition: a vertical segment from the object's position down by depth hits the arena geometry. |
| 122 | `turn_pitch` | f32 speed, f32 target_pitch | next | Turns obj+0x13c towards target_pitch by at most speed*frame_time degrees the shortest way round. |
| 123 | `if_not_in_player_arena` | action: *cond_action* | branch | Condition: the object's arena (obj+0x60) is not the player's arena 0x573a0c. |
| 124 | `path_yaw_offset` | f32 degrees | next | obj+0x100 = degrees, added to the heading derived from the path direction. |
| 125 | `clear_gosub_stack` | — | next | obj+0x248 (gosub depth) = 0: forgets all pending returns. |
| 126 | `aim_pitch_target` | — | next | Pitch obj+0x13c = atan2(target.z + 3 - z, XY distance to the target point). |
| 127 | `if_health` | compare: *compare*, action: *cond_action* | branch | Branches on the object's health. |
| 128 | `attach_effect` | pstr name, u8 slot, u8 kind | next | Only when effects detail 0x5742dc is on. name=="OFF" and slot==kind: removes the effect obj+0x160[slot] (0x405250). Otherwise, if obj+0x160[slot] is empty: obj+0x160[slot] = 0x4067b8(obj, position obj+0x1b0[slot], slot, kind). The name is only compared with "OFF". |
| 129 | `blow_off_parts` | u8 mode, parts: *complex* | next | mode 0/1: 0x45c510 creates a "BLOWOFF" debris effect from the listed parts and sets their bits in obj+0x2c8 and obj+0x2cc (mode 1 also sets flag 8 on the debris); mode 2: same, but only if effects detail 0x5742dc is on (otherwise just skips the names). The helper returns the new pc. |
| 130 | `special_130` | — | next | Calls 0x45d140(obj) (large routine, not analysed). |
| 131 | `special_event` | u8 event | next | event > 50: 0x477cf4(obj, event) (level/cutscene events 0x33, 0x34, 0x35, 0x37, 0x3d, 0x51, 0x5b, 0x5c, 0x5d ...); otherwise global 0x573c80 = -1. |
| 132 | `spawn_effect` | u8 chance, u8 slot, position: *complex* | next | chance < 150: with probability chance% spawns effect 0x406434 at the position; chance >= 150: with probability (chance-150)% spawns effect 0x4052d4 (0.25, 0x30, 0x10) at the object's position + 5 in z (slot position unused). |
| 133 | `arena_texture_frame` | pstr texture_name, u8 mode, f32 value | next | Finds the animated texture texture_name in the arena (arena+0x20 pointer array, arena+0x18 count) and sets its frame: mode 0 absolute frame value-1, mode 1 advance by value*frame_time (frames per second), other modes advance by value. |
| 134 | `turn_roll` | rate (deg/s): *value* | next | Roll/bank angle obj+0x54 += value * frame time, wrapped to [0,360). |
| 135 | `raise_573aa8` | f32 v | next | 0x467f7c: global 0x573aa8 = max(0x573aa8, v) (Kurt-related). |
| 136 | `spawn_debris` | u8 count, f32 vx, f32 vy, f32 vz, f32 spread, u8 absolute, f32 x, f32 y, f32 z, f32 life | next | Spawns count particles (only 1 when effects detail 0x5742dc is off, none if count<=1): template 0x491eb4 with position (x,y,z) (+ object position if absolute==0), velocity (vx,vy,vz) + random*spread*2^-15 (z range biased upward), life = life + rand*0.0005. |
| 137 | `light_group` | u8 tri_group, f32 a, f32 radius, f32 c | next | Relights the arena triangles of group tri_group with the light position/direction left in 0x4d5374/0x4d5358 by a previous opcode 138/139 (a -> 0x4d5370, radius^2*k -> 0x4d5354, c -> 0x4d5364). |
| 138 | `light_group_dir` | u8 tri_group, f32 a, f32 radius, f32 c, f32 light_x, f32 light_y, f32 light_z, f32 dir_x, f32 dir_y, f32 dir_z | next | Sets the light position (0x4d5374..7c) and direction (0x4d5358..60, normalized; zero = omni) and relights triangle group tri_group. |
| 139 | `light_group_point` | u8 tri_group, f32 a, f32 radius, f32 c, f32 light_x, f32 light_y, f32 light_z | next | Like 138 with the direction zeroed (omni light at light_x/y/z). |
| 140 | `group_set_texture` | u8 tri_group, s16 value | next | Stores value into the s16 at +6 (texture/colour index) of every arena triangle of group tri_group. |
| 142 | `fan_create` | u8 hotspot_id, pstr name, u8 param, u8 type, f32 strength | next | Creates a fan (updraft) named name on the arena's type-7 hotspot hotspot_id (fatal error if missing); with type 6 the strength operand is a time and the speed is computed from the hotspot height. |
| 143 | `fan_remove` | pstr name | next | Removes the fan named name from the arena's fan list (fatal error if not found). |
| 144 | `fan_create_box` | pstr name, f32 min_x, f32 min_y, f32 min_z, f32 max_x, f32 max_y, f32 max_z, u8 param, u8 type, f32 strength | next | Creates a fan named name with an explicit box (6 floats) instead of a hotspot; other parameters as in fan_create. |
| 145 | `fan_set_strength` | pstr name, f32 strength, f32 time | next | Changes the strength of fan name to strength, immediately if time == 0, else linearly over time seconds. |
| 146 | `conveyor_create` | u8 tri_group, pstr name, f32 speed, f32 dir_x, f32 dir_y, f32 dir_z, f32 p7, f32 p8 | next | Creates a conveyor (fan list entry of type -1) named name that pushes objects standing on triangles of group tri_group along the normalized direction at speed (see conveyor_push 0x413c80); p7 and p8 are multiplied by 1/\|dir\|. |
| 147 | `conveyor_remove` | pstr name | next | Removes the conveyor/fan named name (same function as fan_remove). |
| 148 | `conveyor_set_speed` | pstr name, f32 speed, f32 time | next | Changes the speed of conveyor/fan name (same function as fan_set_strength). |
| 149 | `spawn_connector` | f32 x, f32 y, f32 z, f32 yaw, s32 instance, pstr type_name, pstr other_arena, off32 script (0 = none) | next | Creates an object (a door) connecting this arena with other_arena: if an object of that type already links the two arenas it is moved into this arena (unless it sits in the player's current/neighbour arena), otherwise a new one is spawned with door defaults (spawn flag 1), yaw, new+0x302 = other arena, flags 0x1108000 and bit masks of its model parts named LOCK (new+0x326) and starting with HC (new+0x32a). Nothing happens if other_arena is NONE. |
| 150 | `door_set_anims` | off32 anim_a (data: animation record, not code), off32 anim_b (data: animation record, not code) | next | Sets the two animations obj+0x306 and obj+0x30a; each offset points to a CMI record that is either the animation data itself or (first u32 == 0) a u32 0 followed by an animation name looked up in the current arena (0x440b88). |
| 151 | `door_set_names` | pstr name0, pstr name1, pstr name2, pstr name3 | next | Stores four string pointers (NULL for NONE) into obj+0x31a, obj+0x322, obj+0x316, obj+0x31e (in that order). |
| 152 | `door_set_flags` | u32 flags | next | obj+0x312 = (obj+0x312 & 0xF) \| (flags & ~0xF): replaces the door flags except their low 4 bits. |
| 153 | `door_set_param` | f32 value | next | Sets the float obj+0x30e (door parameter, 20.0 by default). |
| 154 | `wait_anim_frame` | s16 frame | yield (re-executes this opcode next frame) until the animation reaches the frame | obj+0x108 = address of this opcode (so it is re-executed every frame); continues when the animation is finished (obj+0x114==0 or obj+0x118==0xFF00), or frame>=0 and current frame obj+0xe4 >= frame, or frame==-1 and (obj+0xe4 == end frame obj+0x118 or obj+0x118 < 0); otherwise returns (retry next frame). Scripts mostly use 32767 = wait for the end of the animation. |
| 155 | `if_timer_value` | seconds: *complex*, action: *complex* | branch | Same as opcode 18 with the duration given as a value operand. |
| 156 | `spawn_at_point` | u8 point_index, pstr type_name, off32 script (0 = none) | next | Spawns an enemy of type type_name at this object's point point_index (vec3 at obj+0x1b0 + 12*index). |
| 157 | `vel_away_from` | pstr model_name, s32 id (-1 = any), f32 speed | next | Find the first active object of the own arena with that model name (and id +0x144>>16 unless -1); velocity = normalized(self - it) * speed (a negative speed moves toward it); obj+0xec = 0. Nothing if not found or at the same position. |
| 158 | `touch_damage` | u8 targets, u16 damage, u32 flags, hit_target: *optional* | next, goto on hit, or return if dead | Collision damage using the object's box (obj+0x198): targets bit 0 = the player, bit 1 = other objects (they lose health and get hit events obj+0x21d/0x21e); flags bit 0 = this object dies on a hit. If the object is dead afterwards (obj+8 == 0) the handler returns without saving pc; else on a hit with flags bit 1 it does a goto target. |
| 159 | `spawn_box` | position: *spawn_pos*, f32 size_a, f32 size_b, f32 size_c, pstr name, off32 script (0 = none) | next | Create an object with a box model (model_create_box(name, a, b, c)) in the own arena at: mode 0 the own position, mode 1 own position + local offset (0x45d770), mode 2 the absolute point; object type obj+0x11c = 7, name copied to obj+0x322, sizes to obj+0x316..0x31e, script obj+0x108. |
| 160 | `if_yaw` | compare: *compare*, action: *cond_action* | branch | Branches on the object's yaw. |
| 161 | `spawn_flagged` | f32 x, f32 y, f32 z, pstr type_name, off32 script (0 = none) | next | Spawns type_name at absolute (x,y,z) in this object's arena with the given script; SW_EWJ and BONEFLC become box objects (6x6x6 and 9x9x6, 0x45cca8), other names enemies with the next free instance number (skipped if the type is unknown); the new object's flags (+0x148) are set to 0x2008A6. |
| 162 | `arena_counter_set` | u8 index (1-16), s16 value | next | Sets the arena counter arena+0xcc[(index-1)%16] (s32) to value. |
| 163 | `if_arena_counter` | u8 index (1-16), compare: *compare*, action: *cond_action* | branch | Compares the arena counter with b (and c) and performs the action if the comparison is true. |
| 164 | `path_speed_by_kurt` | u8 enable, params: *complex* | next | enable!=0: obj+0x302..0x30e = the 4 floats, obj+0xe8 = speed_mid, obj+0x14b \|= 0x10: each frame the path speed obj+0xe8 accelerates toward speed_far/mid/near depending on Kurt's distance ahead along the heading compared with distance. enable==0: obj+0x14b &= ~0x10. |
| 165 | `if_no_bomb` | action: *action* | branch | If no World's Most Interesting Bomb is active (0x573c24 == NULL), perform the action. |
| 166 | `if_bomb_visible` | action: *action* | branch | If the Interesting Bomb 0x573c24 exists and the segment from self (z+5) to it (z+5) is not blocked by the BSP of Kurt's arena 0x573a0c (nor of 0x573a68 when 0x573b00 == 0 and 0x573a68 is set), perform the action. |
| 167 | `move_to_bomb` | f32 distance | next | If the Interesting Bomb 0x573c24 exists: destination = bomb position + distance along the direction bomb->self plus a random sideways offset of up to +-min(distance, 6), z = bomb z + 2.5 + obj+0x5c; then start walking like 78 (obj+0x11e = 78). |
| 168 | `group_set_hit_flags` | u8 tri_group (1-16), u8 flags | next | arena+0x6c[(g-1)%16] = flags, the hit behaviour of triangle group g (used by 0x40d560: low bits = hit types that count; 0x80 = destructible: a hit clears triangle flag 0x10 and sets the group bit in arena+0x114; 0x40 = a hit always counts; 0x20 = hit returns 2). With bit 0x80 it also sets flag 0x10 on the group now and its bit in arena+0x110. |
| 169 | `set_roll_radius` | value: *value* | next | obj+0x326 = value: radius of a rolling object (the rolling matrix of 85 turns by distance/(2*pi*radius)*360 degrees; <= 0 means 1). Unaligned; other object kinds use these bytes as a part mask. |
| 170 | `inherit_kurt_vel` | f32 factor | next | Velocity += Kurt's velocity * factor: horizontal from Kurt's local speeds 0x573b0c/0x573b10 rotated by his yaw 0x5739f0 (times 30), vz from his vertical speed 0x573a3c. |
| 171 | `if_is_573c30` | action: *cond_action* | branch | Condition: this object is the one in global 0x573c30 (a player-related object pointer: target/ridden object?). |
| 172 | `explosion` | position: *point*, f32 scale | next | Creates an explosion effect object (with sound) at the position. |
| 173 | `teleport_player_keep` | target: *teleport* | next | Schedules a teleport of the player to arena_name keeping the current position and yaw, or (empty first name) to arena_name2 at the current position/yaw plus (dx,dy,dz,dyaw). Unknown arena = fatal error, script stopped. |
| 174 | `if_ammo` | u8 slot, u8 compare, f32 a, b: *complex*, action: *complex* | branch | Condition: compare(Kurt's counter int[0x5743ef + slot*4] (weapon/ammo counters), a, b) with 0x45da90: 1 <, 2 >, 3 <=, 4 >=, 5 ==, 6 != (0.05 tolerance), 7 a<=v<=b, 8 v<=a or v>=b. |
| 175 | `if_inventory` | u8 item_type, u8 compare, f32 a, b: *complex*, action: *complex* | branch | Condition: compare(sum of counts (+4) of the inventory entries (0x57432c, 0x24 bytes each, 0x5743e0 entries) whose type (+0) == item_type, a, b); compare as opcode 174. |
| 176 | `if_flag_40000` | action: *cond_action* | branch | Condition: bit 0x4 of byte obj+0x14a (0x40000 of the flags dword obj+0x148) is set. |
| 177 | `set_blast_range` | value: *value* | next | obj+0x2c4 = value (float, 1000 by default): blasts farther than this from the object don't hurt it (0x463a94). |
| 178 | `explosion_damage` | position: *point*, f32 scale, f32 damage, f32 radius, u8 flags | next | Creates an explosion effect at the position and applies blast damage round(damage) within radius (0x463a94(pos, damage, radius, 1, 0, flags, -5); flags & 2 = also damage objects of the player's arena). |
| 179 | `spawn_partner` | pstr type_name, off32 script (0 = none) | next | Spawns an enemy of type type_name at this object's position and links both as partners (obj+0x2b8 of each points to the other). |
| 180 | `snap_to_partner` | offset: *optional* | next | If the object has a partner (obj+0x2b8), copies the partner's position into obj+0x10 and adds (dx,dy,dz) when given. |
| 181 | `boss_bar` | source: *boss_bar* | next | HUD health bar (only if 0x573a38 != 0): value = max - arena counter[(index-1)%16] (mode 0) or round((hi - arena var arena+0x48[index]) / (hi - lo) * max) (mode 1), clamped to max; stored in the arena's own object at arena+0x118 (health +0x120, max u16 +0x3ba, active +0x11e = 1); 0x573c78 = that object, 0x573c74 = 1.0 (0 when value <= 0). |
| 182 | `switch_goto` | u8 src, u8 index, repeat(u8 × [off32 target]) | goto | Jump table on (int)variable src/index; if 0 <= v < n it does a goto (obj+0x108 = pc = target). Original bug: the skip loop never advances pc, so it always jumps to entry [1] whatever v is; if v is out of range, execution continues right after entry [0] (table start + 4), not after the table. |
| 183 | `switch_gosub` | u8 src, u8 index, repeat(u8 × [off32 target]) | gosub | Jump table on (int)variable src/index; if 0 <= v < n: gosub entry [v], returning after the whole table. If v is out of range, execution continues at table start + 4 (same bug as 182). |
| 184 | `explode` | f32 damage, f32 radius, f32 height_scale | end | obj+0x148 \|= 0x20; if damage and radius are both non-zero, apply (int)damage of radius damage around the own position; scale the bbox z range (obj+0x1a0, obj+0x1ac) about its middle by height_scale; kill the object (object_kill 0x43d6d4: death script obj+0x110 if set, else the default death) and return from script_run. |
| 185 | `if_speed` | cond: *cond*, action: *action* | branch | If the own current walking speed obj+0x34 passes the condition, perform the action. |
| 186 | `anim_fps_from_speed` | u8 unused, f32 stride | next | obj+0xe0 = obj+0x34 (current speed) * 30 / stride (the u8 is read and ignored). |
| 187 | `random_kick` | f32 horizontal, f32 vertical | next | Velocity += a random horizontal direction (rand(360) degrees) * horizontal * random 0.5..1.5, and vz += vertical * random 0.5..1.5. |
| 188 | `if_target_dist2d` | cond: *cond*, action: *action* | branch | If the horizontal (XY) distance from self to the target point 0x57fc34 passes the condition, perform the action. |
| 189 | `lob_to_kurt` | args: *mode189* | next | If target_z <= own z: solve the fall time t (gravity obj+0x48 * -0.5, current vz obj+0x30) and set the horizontal velocity toward Kurt so the object lands on him: speed = obj+0x44*0.5*t + dist/t, at most max_speed. |
| 190 | `fan_enable` | u8 enable, pstr name | next | Sets (enable != 0) or clears bit 0 of the flags (+0x1c) of the fan/conveyor named name, if it exists. |
| 191 | `if_kurt_delta` | u8 axis (0=x, 1=y, 2=z; +0x80 = signed), cond: *cond*, action: *action* | branch | Take component axis&0x7f of (Kurt's real position 0x5739c0 - own position), as an absolute value unless bit 0x80 is set; if it passes the condition, perform the action. |
| 192 | `if_no_flat_floor_at` | f32 dx, f32 dy, f32 depth, f32 threshold, action: *cond_action* | branch | Condition: no floor below the object-local point (dx,dy), or \|float at +8 of the hit\| (probably the normal's z) < threshold. |
| 193 | `face_mode` | mode: *face_mode* | next | mode 1: yaw = direction of the velocity (obj+0x28, 0x2c) when both components are non-zero; mode 2: the same plus pitch obj+0x13c = atan2(vz, horizontal speed) when both are non-zero; mode 3: copy the yaw of the first active object of the own arena whose model name is name; other modes: nothing. |
| 194 | `group_state_near_player` | u8 op, u8 first_group, u8 last_group, u8 below, u8 above | next | If the group g of the triangle under the player (0x573c10, flags >> 24) satisfies 0 < first <= g <= last: for every group i in first..last applies op (as in 98, on the player's arena 0x573a0c) when g-below <= i <= g+above and the opposite op (0<->1, 2<->3, 4<->5) otherwise, only where the arena masks say the state changes. |
| 195 | `if_hit_weapon` | s8 weapon, action: *complex* | branch | Condition: s8 obj+0x21d (type of what caused the last hit: projectile type 1..4, 0xFC/0xFD for other damage) == weapon. |
| 196 | `set_camera_limit_yield` | value: *value* | yield (returns without saving pc; next frame restarts at obj+0x108) | Sets the float at player-arena (0x573a0c)+0x462 (limit used by 0x4612e0) and returns from script_run without saving pc, so the next frame restarts at obj+0x108. |
| 197 | `find_advance_spot` | action: *action* | branch | Like 46 without the visibility tests and with 2D distances (5..400): a type-5 waypoint no farther from self than Kurt and closer to Kurt than self becomes the destination with obj+0x11e = 197, else obj+0x11e = 0. If obj+0x11e == 197, perform the action. |
| 198 | `set_weak_parts` | pstr part_prefix, u8 prefix_len, u32 hit_points, u32 unknown | next | obj+0x149 \|= 0x20; obj+0x302 = part name prefix, obj+0x306 = number of characters compared; the 8 per-part hit counters u16 obj+0x30e[i] and u16 obj+0x31e[i] (max) = hit_points (low 16 bits); obj+0x30a = unknown. Parts whose name matches the prefix take damage individually (see 0x462384 users). |
| 199 | `set_104` | value: *value* | next | obj+0x104 = value (float). |
| 200 | `move_to_point` | f32 speed, f32 x, f32 y, f32 z, action: *cond_action* | branch | Sets the per-axis velocities obj+0x294/0x298/0x29c to move towards (x,y,z) at speed (z ignored when obj+0x148 bit 2) and performs the action once the Manhattan distance is < 0.5. |
| 201 | `push_dir` | f32 speed, f32 angle_offset (deg) | next | Horizontal move accumulator (obj+0x294, obj+0x298) += speed * (cos, sin)(yaw + angle_offset). |
| 202 | `set_574304` | s8 value | next | Global 0x574304 = value (sign-extended byte). |
| 203 | `camera_track` | height: *optional* | next | Moves global 0x573918 (probably the player's camera pitch) towards this object when the player faces it; mode 0 uses obj+0x1ac, mode 1 obj.z + factor*obj+0x58, limited by player-arena+0x462. Sets 0x5739b0 = 1. |
| 204 | `avoid_objects` | — | next | If the object's box overlaps another active object, steers its yaw (obj+0x4c) away from it. |
| 205 | `set_21f` | u8 value | next | obj+0x21f = value. |
| 206 | `spawn_along_path` | off32 path (data: spline keys, not code), f32 step, pstr type_name, off32 script (0 = none) | next | Spawns one enemy of type type_name every 'step' time units along the spline at g_cmi+path (u32 count, then count 40-byte keys whose first s32 is the key time) from t=0 up to the last key's time, with consecutive instance numbers; each gets flags 0x2008A6. |
| 207 | `turn_to_yaw` | f32 speed, f32 target_yaw, action: *cond_action* | branch | Turns obj+0x4c towards target_yaw by speed*frame_time (0x460968) and normalizes it to [0,360); unless act == 0xFF performs the action when the yaw equals target_yaw (0xFE: else-gosub otherwise). |
| 208 | `if_part_hit` | pstr part_name, action: *cond_action* | branch | Condition: the model part part_name exists and its bit is set in obj+0x2c8 (hit/destroyed parts mask); when true the action is performed and obj+0x21e is cleared. |
| 209 | `if_player_on_me_low` | action: *cond_action* | branch | Condition: the player stands on this object (0x573b84 == obj), 0x573a38 != 0 and the player's z <= 0x573a1c. |
| 210 | `set_2c0` | value: *value* | next | obj+0x2c0 = value (float). |
| 211 | `stop_velocity` | — | next | Clears obj+0x28, obj+0x2c, obj+0x30 (velocity). |
| 212 | `if_hit_fd` | action: *complex* | branch | Condition: hit event obj+0x21e == 0xFD (-3, set by 0x45cf60, area/blast hit). When true, obj+0x21e is cleared after the action. |
| 213 | `if_partner_distance` | compare: *compare*, action: *cond_action* | branch | Branches on the distance to the partner object. |
| 214 | `if_partner_angle` | compare: *compare*, action: *cond_action* | branch | Branches on the relative angle to the partner object. |
| 215 | `screen_flash` | intensity: *value* | next | Screen flash: global 0x573b68 = max(0x573b68, round(intensity)) (it fades by 4 per tick). |
| 216 | `add_var_dt` | u8 src, u8 index, f32 rate | next | Variable src/index += rate * frame time (0x491e24). |
| 217 | `global_573c4c_add` | u8 mode, s32 value | next | If mode == 1: global 0x573c4c += value; other modes do nothing. |
| 218 | `set_f13c` | f32 v | next | obj+0x13c = v (smoothed pitch/bank value used when turning along a path). |
| 219 | `if_check_461024` | f32 a, f32 b, action: *cond_action* | branch | Condition: helper 0x461024(obj, a, b) (uses the player's velocity, random numbers and other objects near y=a/z=b; exact meaning unclear). |
| 220 | `place_x_near_player` | f32 x_min, f32 x_max, f32 y_limit | next | Sets obj.x (obj+0x10) to a random or player-predicted x in [x_min, x_max] (uses the player's x and speed when he is inside the range), avoiding other objects on the same y. |
| 221 | `pick_waypoint8` | u8 mode (1 = only the group of the nearest type-8 point), action: *action* | branch | Pick a random weighted type-8 waypoint (mode 0: 50..500 units away (2D); mode 1: ids within +-3 of the nearest type-8 point); on success it becomes the destination with obj+0x11e = 221, else obj+0x11e = 0. If obj+0x11e == 221, perform the action. |
| 222 | `if_opcode_count` | compare: *compare*, action: *cond_action* | branch | Branches on the per-call opcode counter (the one that raises 'Alien %s looped' above 1000). |
| 223 | `arena_set_neighbour` | pstr arena_name | next | Sets the secondary (neighbour) arena 0x573a68 to arena_name and starts loading it (NONE or same = no change). |
| 224 | `wind_zone` | params: *optional* | next | enable: if the player is inside a type-9 hotspot box of the current arena, sets 0x573bec = 1 and, if 0x573be8 or 0x573c14 is set, turns the player to yaw and pushes him at speed (0x468be0), else lowers his vertical speed 0x573a3c. enable == 0 clears 0x573be8 and 0x573a80. |
| 225 | `if_kurt_on_floor` | action: *action* | branch | If Kurt currently stands on a floor polygon (0x573c10 != NULL), perform the action. |
| 226 | `jump_to` | f32 x, f32 y, f32 z, f32 p0, f32 p1 | next | Sets up a jump/ballistic move: obj+0x1c..0x24 = (x,y,z), obj+0x302 = p0, obj+0x30a = p1, obj+0x30e = current yaw, obj+0x306 = z - obj.z, and sets bit 0x40 of obj+0x14a. |
| 227 | `partner_flank` | off32 target_close, off32 target_flank | branch (goto target_close / goto target_flank / next) | AI pair manoeuvre: without a partner continues. Computes the angle between this object and its partner as seen from the player; > 120 degrees: continues; < 45 and the object sees the player (0x4605d0), has floor at -42 and no wall ahead: goto target_close; otherwise sets a move target 20 units away at +-60 degrees (obj+0x120..0x128, movement command obj+0x11e = '+') and goto target_flank. Offsets are not null-checked. |
| 228 | `link_partners` | type_name: *pstr_or_flag* | next | With a name: pairs up active objects of that name in the arena that have no partner (obj+0x2b8) and are closer than 100 units (squared distance < 10000), linking each pair mutually. With L==0 and flag 0: clears this object's partner pointer. |
| 229 | `turn_and_jump_to_dest` | f32 turn_rate (deg/s), action: *action* | branch | Turn the yaw toward the destination obj+0x120 by at most turn_rate*dt; once facing it start a ballistic jump (obj+0x11e = 229, velocity obj+0x28..0x30 from obj+0x44/obj+0x48, flight time in obj+0x302). If obj+0x11e == 229, perform the action. |
| 230 | `spawn_ex` | f32 x, f32 y, f32 z, f32 yaw, s32 instance (<0 = next free), pstr type_name, off32 script (0 = none) | next | Spawns an enemy of type type_name at absolute (x,y,z) with the given instance number (next free one if negative) and sets its yaw (new+0x4c). |
| 231 | `if_move_idle_flag` | action: *complex* | branch | Condition: obj+0x11e == 0 (no movement command running) and byte obj+0x2a0 != 0 (movement result flag set by the movement code; opcode 43 clears it). |
| 232 | `if_option` | u8 value, action: *cond_action* | branch | Condition: global 0x5742dc == value (a 0/1 option toggled by cheat codes, default 1; also picks BHOLE vs BHOLE2). |
| 233 | `if_sound_playing` | pstr sound, action: *complex* | branch | Condition: the named sound (looked up in the sound list 0x4d2e20) currently has a playing voice. |
| 234 | `if_target_angle_signed` | cond: *cond*, action: *action* | branch | Own yaw minus the direction to the target point, normalized to (-180,180]; if it passes the condition, perform the action. |
| 235 | `turn_to_target_offset` | f32 turn_rate (deg/s), f32 accuracy (100 = exact), f32 offset_a, f32 offset_b | next | P = target - (a*cos(ty) + b*sin(ty), b*cos(ty) + a*sin(ty)), ty = target yaw 0x57fc30; the angle to P gets a random error (rand(20)-10)*(100-accuracy)/(1+dist2d) unless accuracy == 100; turn the yaw toward it by at most turn_rate*dt. |
| 236 | `if_no_floor_at` | f32 dx, f32 dy, f32 depth, action: *cond_action* | branch | Condition: no floor below the object-local point (dx,dy) (rotated by yaw) within depth (a default depth if 0). |
| 237 | `if_self_in_box` | f32 x_min, f32 y_min, f32 z_min, f32 x_max, f32 y_max, f32 z_max, action: *action* | branch | If the own position obj+0x10 is inside the axis-aligned box, perform the action. |
| 238 | `if_self_pos` | u8 axis (0=x, 1=y, other=z), cond: *cond*, action: *action* | branch | If the own position component obj+0x10/0x14/0x18 passes the condition, perform the action. |
| 239 | `set_27c` | f32 a0, f32 a1, f32 a2, f32 b0, f32 b1, f32 b2 | next | Stores six floats into obj+0x27c..obj+0x290 (probably two vectors; meaning unknown). |
| 240 | `set_hurt_flash` | u8 value | next | Hurt flash 0x573b20 = value; the value 1 means 5 and is ignored while Kurt is invulnerable (0x573bd4 > 0). |
| 241 | `if_player_health` | compare: *compare*, action: *cond_action* | branch | Branches on the player's health. |
| 242 | `set_2d0_block` | params: *optional* | next | enable != 0: obj+0x2d0 = 1, obj+0x2d1 = 0xFF and the 12 floats go to obj+0x2d2..obj+0x2fe; enable == 0: obj+0x2d1 = 0. |
| 243 | `if_point_sees_player` | u8 point_index, u16 range, action: *cond_action* | branch | Condition: the player is within range of the object's point point_index (obj+0x1b0 + 12*index) and the line between them is not blocked. |
| 244 | `set_global_573bd4` | u8 add, f32 v | next | add==0: global float 0x573bd4 = v; otherwise 0x573bd4 += v. |
| 245 | `find_object` | args: *find_object* | next | Linked object obj+0x2b8 = an object of the own arena (not self) with that model name, or NULL: mode 0 a random one, 1 the nearest (3D), 2 the nearest within 200 that has obj+0x149 bit 4, is at most 9 units higher and is in line of sight. |
| 246 | `turn_to_linked` | u8 flags (bit 0 = turn to the linked object, bit 7 = also pitch), f32 turn_rate (deg/s) | next | Only when flags bit 0 is set and the linked object obj+0x2b8 exists: turn the yaw toward it by at most turn_rate*dt; with bit 7 also turn the pitch obj+0x13c toward atan2(dz + obj+0x5c, XY distance). |
| 247 | `hud_message` | u8 flags, pstr name, f32 time | next | Queues the resource name (looked up by 0x415620) in the 4-entry display queue at 0x57ecf0 with flags and time (flags & 2 = insert at the front); most likely an on-screen message/picture. |
| 248 | `push_kurt` | args: *mode248* | next | Knock Kurt back (sniper mode is left first): unless 0x573a80 >= 9 or Kurt's state == 800, set his state to 901 and 0x573a80 = 9, add a horizontal push to 0x573c08/0x573c0c (mode 0: (-a*cos(yaw) - b*sin(yaw), -b*cos(yaw) - a*sin(yaw)) * dt; other modes: a * dt along the direction self->Kurt) and add up to his vertical speed 0x573a3c. |
| 249 | `if_own_sound` | u8 mode, action: *complex*, sound: *complex* | branch | Condition on the tracked sound voice obj+0x158 (set by opcode 89 flag 4): mode 0: obj+0x158 != 0; mode 1: obj+0x158 != 0 and its name obj+0x15c equals the sound name; other modes: obj+0x158 == 0. |
| 250 | `if_in_box` | u8 mask, type_name: *complex*, u8 mode, box: *complex*, action: *complex* | branch | Condition: mask==0xFF: some active object of that type in the arena is inside the box; otherwise one of the 3 player projectile slots (0x573c98, 0xfc bytes each) with type t (slot+0xd0, 1..4) and mask bit 1<<(t-1) is inside the box (slot position +0x20). Tests xmin<=x<=xmax and ymin<=y<=ymax, plus zmin<=z<=zmax only when mode==3 (order confirmed by LEVEL4/LEVEL7 data; the 8 boxes in LEVEL7 DANT_6 have an empty z range and never pass). |
| 251 | `set_target_mode` | u8 mode | next | obj+7 = mode: 1 = this object is the global alien target (0x491e48); 2 = this object always targets Kurt (target position 0x57fc34 = Kurt's position/yaw now). mode 0 releases 0x491e48 if it was this object. |
| 252 | `gosub` | repeat(u8 × [off32 target]) | gosub | Gosub to one of N targets chosen at random: pushes (pc after the operands, obj+0x108) on the 4-deep stack obj+0x24c/obj+0x25c, depth obj+0x248++, then obj+0x108 = pc = target, obj+0x26c[depth] = 0. Overflow (depth>=4): debug log, obj+0x108 = 0, return. |
| 253 | `return` | — | return | Return from gosub: depth--, pc = obj+0x24c[depth], obj+0x108 = obj+0x25c[depth]. Underflow (depth 0): debug log, obj+0x108 = 0, return. |
| 255 | `end_frame` | — | end | Handled by the interpreter loop (0x440ce3): clears the hit event obj+0x21e and returns; obj+0x108 is unchanged so the next frame restarts at the last restart point. |
